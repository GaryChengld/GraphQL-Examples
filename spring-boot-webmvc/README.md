# GraphQL Spring Boot Web-mvc Example

### Build
```
mvn clean install
```

### To run locally
```
mvn spring-boot:run
```

### To run in docker
```
docker-compose up -d --build
```

### To stop in docker
```
docker-compose down
```
