version: '3.7'
services:
  app:
    build: .
    image: "examples/spring-boot-webmvc-graphql-example"
    container_name: "spring-boot-webmvc-graphql-example"    
    ports:
      - "8000:8000"
    environment:
      TZ: Canada/Eastern